import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const Color backgroundDark = Color(0xFF090A0F);
  static const Color primaryPurple = Color(0xFF8554F8);

  static const Color textMain = Color(0xFFF8FAFC);
  static const Color textMuted = Color(0xFF94A3B8);

  static const Color idleGrey = Color(0xFF64748B);
  static const Color accentPhysics = Color(0xFF3B82F6);
  static const Color accentMaths = Color(0xFFF43F5E);

  // Equivalent to white with 4% and 10% opacity.
  static const Color glassBackground = Color(0x0AFFFFFF);
  static const Color glassBorder = Color(0x1AFFFFFF);
}
