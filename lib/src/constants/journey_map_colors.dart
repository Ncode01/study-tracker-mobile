import 'package:flutter/material.dart';

/// Color constants for the Journey Map theme
/// These colors are extracted from the design image to ensure pixel-perfect implementation
class JourneyMapColors {
  /// Primary background color - warm beige
  static const Color background = Color(0xFFF0E8D9);

  /// Primary text color - dark brown
  static const Color primaryText = Color(0xFF5C3A21);

  /// Secondary text color - medium brown
  static const Color secondaryText = Color(0xFF8B6B47);

  /// Card background color - off-white with warm tint
  static const Color cardBackground = Color(0xFFFFF9F0);

  /// Card border color - soft brown
  static const Color cardBorder = Color(0xFFD4C4A8);

  /// Card icon background - light brown/beige
  static const Color cardIconBackground = Color(0xFFE8DCC6);

  /// Tab indicator color - dark brown (for active tab underline)
  static const Color tabIndicator = Color(0xFF5C3A21);

  /// Tab inactive text color - light brown
  static const Color tabInactive = Color(0xFFB8A082);

  /// Button background color - blue (for "Add New Stop" button)
  static const Color buttonBackground = Color(0xFF7BB3D9);

  /// Button text color - white
  static const Color buttonText = Color(0xFFFFFFFF);

  /// Button border color - darker blue
  static const Color buttonBorder = Color(0xFF5A9BC4);

  /// Map background color - light gray/beige for map area
  static const Color mapBackground = Color(0xFFF5F2ED);

  /// Accent color for icons and highlights
  static const Color accent = Color(0xFF7BB3D9);

  /// Error/warning color
  static const Color error = Color(0xFFD32F2F);

  /// Success color
  static const Color success = Color(0xFF4CAF50);
}
